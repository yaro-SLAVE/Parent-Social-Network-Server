from app.settings import *
from pymongo import MongoClient
from mongoengine import connect

for ms sql

DATABASES = {
    'default':{
        'ENGINE':'mssql',                    
        'NAME':'db_name',                       
        'HOST':'server_name', 
        'OPTIONS': {
            'driver': 'ODBC Driver 17 for SQL Server',
        },
    }
}

for mongo db

MONGO_HOST = ''
MONGO_DB = ''
connect(MONGO_DB, host=MONGO_HOST)

for data-migrations from other DB
MONGO_CLIENT = MongoClient('')
MONGO_DB = MONGO_CLIENT['']